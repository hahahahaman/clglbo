clglbo - common lisp + opengl = breakout

interactions:
- ball, paddle must stay inside screen
- when ball hits something it changes direction
- when ball hits soft brick that brick is destroyed
- when all soft bricks destroyed load next level
